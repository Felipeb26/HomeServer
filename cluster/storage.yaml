# storage gerenciado via hostPath direto nos deployments
# drives disponíveis:
# /mnt/big_storage  — HDD SATA 1.8TB — mídia e downloads
# /mnt/storage_ext  — SSD USB 465GB  — configs e I/O frequente
# /mnt/tank-hd      — HDD USB 465GB  — dados de aplicações
# /mnt/ext_ssh_bk   — HDD SATA 465GB — backup e logs
