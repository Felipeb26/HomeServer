# storage gerenciado via hostPath direto nos deployments
# drives disponíveis:
# /mnt/seagate-2tb   — HDD USB 2TB      — mídia, downloads, configs
# /mnt/storage_ext   — SSD USB 465GB    — configs e I/O frequente
# /mnt/red-wd        — HDD SATA 2TB     — torrents, immich, dados
# /mnt/backup-500gb  — HDD SATA 500GB   — arquivos de backup
