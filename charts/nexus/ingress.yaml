---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-proxy-headers
  namespace: development
data:
  X-Forwarded-Proto: "https"
  X-Forwarded-Port: "443"
---  
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nexus-ingress
  namespace: development
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "900"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "900"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - nexus.batsworks.com.br
        - registry.batsworks.com.br
      secretName: nexus-tls
  rules:
    - host: nexus.batsworks.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus
                port:
                  number: 8081
    - host: registry.batsworks.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus
                port:
                  number: 5000
