apiVersion: apps/v1
kind: Deployment
metadata:
  name: fileflows
  namespace: media
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: fileflows
  template:
    metadata:
      labels:
        app: fileflows
    spec:
      securityContext:
        supplementalGroups:
          - 992
          - 44
      runtimeClassName: nvidia
      containers:
        - name: fileflows
          image: revenz/fileflows:26.06
          ports:
            - containerPort: 5000
          env:
            - name: TZ
              value: America/Sao_Paulo
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TempPathHost
              value: /tmp/fileflows
            - name: NVIDIA_VISIBLE_DEVICES
              value: all
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: compute,utility
            - name: MACHINE_LEARNING_DEVICE
              value: cuda
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 3000m
              memory: 4Gi
              nvidia.com/gpu: 1
          livenessProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 5
          readinessProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          volumeMounts:
            - name: data
              mountPath: /app/Data
            - name: logs
              mountPath: /app/Logs
            - name: common
              mountPath: /app/common
            - name: media
              mountPath: /media
            - name: mp4
              mountPath: /mp4
            - name: temp
              mountPath: /temp
            - name: docker-sock
              mountPath: /run/k3s/containerd/containerd.sock
            - name: dri
              mountPath: /dev/dri
            - name: ffmpeg
              mountPath: /usr/bin/ffmpeg
            - name: ffprobe
              mountPath: /usr/bin/ffprobe
            - name: ffmpeg-libs
              mountPath: /usr/lib/x86_64-linux-gnu
      volumes:
        - name: data
          hostPath:
            path: /mnt/seagate-2tb/fileflow/data
            type: DirectoryOrCreate
        - name: logs
          hostPath:
            path: /mnt/seagate-2tb/fileflow/logs
            type: DirectoryOrCreate
        - name: common
          hostPath:
            path: /mnt/seagate-2tb/fileflow/common
            type: DirectoryOrCreate
        - name: media
          hostPath:
            path: /mnt/red-wd/Torrent
            type: DirectoryOrCreate
        - name: mp4
          hostPath:
            path: /mnt/red-wd/immich/library
            type: DirectoryOrCreate
        - name: temp
          hostPath:
            path: /tmp/fileflows
            type: DirectoryOrCreate
        - name: docker-sock
          hostPath:
            path: /run/k3s/containerd/containerd.sock
            type: Socket
        - name: dri
          hostPath:
            path: /dev/dri
        - name: ffmpeg
          hostPath:
            path: /usr/bin/ffmpeg
            type: File
        - name: ffprobe
          hostPath:
            path: /usr/bin/ffprobe
            type: File
        - name: ffmpeg-libs
          hostPath:
            path: /usr/lib/x86_64-linux-gnu
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: fileflows
  namespace: media
spec:
  selector:
    app: fileflows
  ports:
    - port: 5000
      targetPort: 5000
